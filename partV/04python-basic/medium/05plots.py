import numpy #as np
import pylab #as pl

# Make an array of x values
x1 = [1, 2, 3, 4, 5]
# Make an array of y values for each x value
y1 = [1, 4, 9, 16, 25]

# use pylab to plot x and y
pylab.plot(x1, y1)
pylab.show()
